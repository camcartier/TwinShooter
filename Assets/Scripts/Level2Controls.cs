using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Level2Controls : LevelControls
{

    void Awake()
    {
        _totalNumberOfMobInLevel = 40;
        _totalNumberOfPirangnaInLevel = 32;
        _totalNumberOfTurretsInLevel = 8;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
